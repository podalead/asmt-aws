apiVersion: v1
kind: Service
metadata:
  namespace: asmt-aws
  name: asmt-aws-test-service
spec:
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
  type: NodePort
  selector:
    app.kubernetes.io/name: asmt-aws-test-app
